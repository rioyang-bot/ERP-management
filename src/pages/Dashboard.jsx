import React from 'react';

const Dashboard = () => {
  return (
    <div className="card-surface">
      <h1 className="page-title">總覽 (Dashboard)</h1>
      <p style={{ color: 'var(--text-muted)' }}>系統統計與摘要即將在此呈現。</p>
    </div>
  );
};

export default Dashboard;
