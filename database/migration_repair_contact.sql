-- ============================================================================
-- 維修單：記錄聯絡人
-- ----------------------------------------------------------------------------
-- 同一家公司往往有多位聯絡人，先前維修單只存客戶名稱，
-- 建單時既看不到聯絡人、事後也查不出當初是對誰處理的。
--
-- 具冪等性，可重複執行。
-- ============================================================================

ALTER TABLE repair_orders ADD COLUMN IF NOT EXISTS contact_person VARCHAR(100);
ALTER TABLE repair_orders ADD COLUMN IF NOT EXISTS contact_phone VARCHAR(50);
