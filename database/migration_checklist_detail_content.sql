-- ============================================================================
-- 出機檢查表：細項改為填寫「內容」
-- ----------------------------------------------------------------------------
-- 細項記錄的是這台設備的實際內容而不是做完沒有，例如細項名稱「OS」、
-- 內容「RH9.6」，因此不勾選、改為填值。主要檢查功能維持勾選。
--
-- is_checked 欄位保留不動：主要檢查功能仍在用，既有資料也不必搬動。
-- 具冪等性，可重複執行。
-- ============================================================================

ALTER TABLE asset_checklist_items ADD COLUMN IF NOT EXISTS content TEXT;
